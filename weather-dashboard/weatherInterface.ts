// Interface (weatherInterface.ts): Define an interface for weather data that
// includes properties such as temperature, humidity, weather condition, and city name.


export interface WeatherInterface {
  temperature: number;
  humidity: number;
  weatherCondition: string;
  city: string;
}